=Adding flag
src/main.ml  add name of feature to features list (start it with capitol letter)
Makefile.in CILLY_MODULES  add the name of the feature (no capitol letter)
	configure
	make
	(test by typing cilly and checking whether do"feature" is in there...)


=Making changes to ml file
In this directory:
	scons -U ../cil		# build just cilly
or perhaps:
	scons -D		# build whole project
=Running
To get xml file:
../cil/bin/cilly --dowpds inter_simple4.c
