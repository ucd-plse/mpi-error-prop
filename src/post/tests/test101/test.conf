# this test should be enabled for LLVM
# failing reason: if (err) not accounted as "checked"
# Is this test a good candidate for irllvm?
# Not sure that counting if (err) as acknowledged is desired behavior there.

[create wpds]
enabled = cil

[create wpds]
sign = positive
enabled = cil
